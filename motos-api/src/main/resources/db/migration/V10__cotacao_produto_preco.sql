CREATE TABLE cotacao (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    data VARCHAR(10) NOT NULL,
    usd_pyg DOUBLE PRECISION NOT NULL,
    brl_pyg DOUBLE PRECISION NOT NULL,
    status VARCHAR(20) NOT NULL DEFAULT 'ativo'
);

CREATE UNIQUE INDEX cotacao_data ON cotacao (data);

ALTER TABLE produto ADD COLUMN aliquota_iva INTEGER NOT NULL DEFAULT 10;
ALTER TABLE produto ADD COLUMN moeda_preco VARCHAR(3) NOT NULL DEFAULT 'usd';
ALTER TABLE produto ADD COLUMN preco_lista DOUBLE PRECISION NOT NULL DEFAULT 0;
ALTER TABLE produto ADD COLUMN custo DOUBLE PRECISION NOT NULL DEFAULT 0;
